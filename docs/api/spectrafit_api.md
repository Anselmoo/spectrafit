# SpectraFit Command Surface

The v2 public entry surface is organized around CLI/runtime modules rather than
the removed `spectrafit.spectrafit` monolith.

## CLI application

::: spectrafit.cli.main

## CLI runtime/config resolution

::: spectrafit.cli.runtime
